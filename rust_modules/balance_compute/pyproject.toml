[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "balance_compute"
version = "0.1.0"
description = "TallyBook balance computation and ledger processing (Rust native)"
requires-python = ">=3.10"