[package]
name = "balance_compute"
version = "0.1.0"
edition = "2021"

[lib]
name = "balance_compute"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.23", features = ["extension-module"] }